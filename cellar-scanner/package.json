{
  "name": "cellar-scanner",
  "version": "1.0.0",
  "private": true,
  "description": "Cellar Scanner — camera tin barcode, date OCR, CSV export (web + Capacitor Android).",
  "scripts": {
    "sync": "cap sync",
    "open:android": "cap open android",
    "android:debug": "cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "7.6.5",
    "@capacitor/core": "7.6.5"
  },
  "devDependencies": {
    "@capacitor/cli": "7.6.5"
  },
  "license": "MIT"
}
